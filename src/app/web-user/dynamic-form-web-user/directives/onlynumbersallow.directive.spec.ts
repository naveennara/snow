import { OnlynumbersallowDirective } from './onlynumbersallow.directive';

describe('OnlynumbersallowDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlynumbersallowDirective();
    expect(directive).toBeTruthy();
  });
});
