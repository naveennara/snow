import { LargeTextElipsePipe } from './large-text-elipse.pipe';

describe('LargeTextElipsePipe', () => {
  it('create an instance', () => {
    const pipe = new LargeTextElipsePipe();
    expect(pipe).toBeTruthy();
  });
});
