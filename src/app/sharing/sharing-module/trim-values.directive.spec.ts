import { TrimValuesDirective } from './trim-values.directive';

describe('TrimValuesDirective', () => {
  it('should create an instance', () => {
    const directive = new TrimValuesDirective();
    expect(directive).toBeTruthy();
  });
});
